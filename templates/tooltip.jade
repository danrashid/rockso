p #{start} –
  br
  |  #{end}

table
  each layer in layers
    tr
      td(style="color:#{layer.color}") #{layer.guid}
      td(align="right") #{layer.percent}%
      td= layer.value
  tr
    td(align="right" colspan="2") Total
    td= total
